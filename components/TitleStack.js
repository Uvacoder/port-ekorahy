import React from 'react';

function TitleStack({ children }) {
  return <h2 className="font-medium text-base font-Quicksand text-center text-secondary py-2 lg:text-lg">{children}</h2>
}

export default TitleStack;